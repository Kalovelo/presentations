services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: splitdumb
      POSTGRES_USER: splitdumb
      POSTGRES_PASSWORD: splitdumb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
